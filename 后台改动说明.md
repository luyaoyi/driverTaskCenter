# 后台改动说明

---

## 改动一：奖品类型改造 + 现金奖励 + 任务标签

**变化类型：修改**

### 奖励配置区域

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
|---------|---------|---------|---------|-------|--------------|
| 奖励类型 | 枚举 | 单选框 | 是 | 普通奖品 | 支持普通奖品（normal）、现金奖励（cash）二选一；三种任务类型（完单/发布行程/添加线路）均支持现金奖励 |
| 普通奖励领取 code | 字符串 | 单行文本输入框 | 条件必填 | 无 | 当奖励类型为「普通奖品」时必填；去除首尾空格后不能为空 |
| 奖励金额 | 整数 | 数值输入框 | 条件必填 | 1 | 当奖励类型为「现金奖励」时必填；数值范围 1～100，仅支持正整数；校验失败提示「现金奖励金额需在1～100之间的正整数」 |
| 现金奖励 code | 字符串 | 单行文本输入框 | 条件必填 | 无 | 当奖励类型为「现金奖励」时必填；去除首尾空格后不能为空；校验失败提示「请填写现金奖励 code」 |

### 现金奖励适用范围限制

> **调整：** 完单任务恢复支持现金奖励，不再做任务类型层面的限制。

| 任务类型 | 是否支持现金奖励 | 说明 |
|---------|---------------|------|
| 完单任务（finish_order） | 是 | 可选现金奖励（完单 1~n 单达标后领取） |
| 发布行程任务（publish_trip） | 是 | 可选现金奖励 |
| 添加线路任务（add_route） | 是 | 可选现金奖励 |

**说明：**
- 任务类型切换时不再自动重置奖励类型
- 任务周期切换为「冷却周期型」（强制任务类型为完单任务）时，不再自动将现金奖励切回普通奖品
- 提交时不再拦截「完单任务 + 现金奖励」组合

### 前端样式配置区域（新增）

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
|---------|---------|---------|---------|-------|--------------|
| 任务标签 | 枚举 | 单选框 | 否 | 无 | 支持「无」（空字符串）、「新手任务」（newbie_task）两个选项；默认不选中任何标签 |

### 表格列展示

| 字段名称 | 展示方式 | 说明 |
|---------|---------|------|
| 奖励类型 | Tag 标签 | 普通奖品显示绿色 Tag；现金奖励显示红色 Tag 并带金额，如「现金 ¥20」 |
| 任务标签 | Tag 标签 | 新手任务显示橙色 Tag；无标签时显示 `-` |

### 详情抽屉奖励信息

| 场景 | 展示方式 |
|------|---------|
| 普通奖品 | 绿色卡片，显示发放结果 Tag、奖励 Code、重试按钮 |
| 现金奖励 | 黄色卡片，显示发放结果 Tag、奖励金额（红色加粗）、现金奖励 Code、重试现金发放按钮 |

### 涉及接口字段

| 字段名 | 类型 | 说明 |
|-------|------|------|
| rewardType | `'normal' \| 'cash'` | 奖励类型（单选） |
| normalRewardCode | string | 普通奖励领取 code |
| cashAmount | number | 现金奖励金额（1～100） |
| cashCode | string | 现金奖励发放 code |
| taskTag | `'' \| 'newbie_task'` | 任务标签 |

---

## 改动二：投放限制 - 司机状态限制

**变化类型：新增**

### 投放限制区域

| 字段名称 | 数据类型 | 页面控件 | 是否必填 | 默认值 | 配置与校验规则 |
|---------|---------|---------|---------|-------|--------------|
| 启用司机状态限制 | 布尔 | 开关 | — | 关闭 | 开启后展开司机状态条件选择 |
| 司机状态条件 | 枚举 | 单选框 | 条件必填 | 认证未发布行程 | 当开关开启时必填；支持「认证未发布行程」（no_trip_published）、「认证未添加线路」（no_route_added）二选一 |

### 配置逻辑说明

- 仅针对**认证司机**生效，过滤未满足指定状态条件的司机
- 当限制开启且条件为「认证未发布行程」时，仅允许已认证但尚未发布行程的司机参与任务
- 当限制开启且条件为「认证未添加线路」时，仅允许已认证但尚未添加线路的司机参与任务
- 开关关闭时不做任何状态过滤

### 涉及接口字段

| 字段名 | 类型 | 说明 |
|-------|------|------|
| enableDriverStatusLimit | boolean | 是否启用司机状态限制 |
| driverStatusCondition | `'no_trip_published' \| 'no_route_added'` | 司机状态条件 |

---

## 改动三：任务参与记录 - 新增任务类型列 + 现金打款状态

**变化类型：新增**

### 参与记录列表表格

| 字段名称 | 展示方式 | 说明 |
|---------|---------|------|
| 任务类型 | Tag 标签（info plain） | 完单任务 / 发布行程 / 添加线路 |
| 奖励发放 | Tag 标签组合 | 普通奖品：单行显示发放状态（已发放/发放失败/未发起）；现金奖励：双行显示「发放状态」+「打款状态」 |

### 打款状态说明（现金奖励专属）

| 状态值 | 展示文案 | Tag 类型 | 说明 |
|-------|---------|---------|------|
| none | 未发起 | info | 现金奖励尚未发起发放，打款流程未启动 |
| pending | 打款中 | warning | 发放已发起，资金打款处理中 |
| success | 打款成功 | success | 资金已成功打入司机账户 |
| fail | 打款失败 | danger | 打款失败，可重试 |

### 详情抽屉

| 区域 | 改动内容 |
|------|---------|
| 描述列表 | 新增「任务类型」字段，显示 Tag 标签 |
| 现金奖励卡片 | 发放结果拆分为两个 Tag：「发放：xxx」+「打款：xxx」 |

### 涉及接口字段

| 字段名 | 类型 | 说明 |
|-------|------|------|
| taskType | `'finish_order' \| 'publish_trip' \| 'add_route'` | 任务类型（参与记录） |
| cashPayStatus | `'none' \| 'pending' \| 'success' \| 'fail'` | 现金打款状态 |

---

## 改动汇总

| 序号 | 模块 | 改动项 | 改动类型 |
|-----|------|-------|---------|
| 1 | 奖励配置 | 奖品类型由多选改为单选 | 修改 |
| 2 | 奖励配置 | 新增现金奖励选项（含金额 + code） | 新增 |
| 3 | 奖励配置 | 现金奖励支持三种任务类型（完单/发布行程/添加线路） | 修改 |
| 4 | 前端样式 | 新增任务标签单选（新手任务） | 新增 |
| 5 | 表格展示 | 新增奖励类型列（含现金展示） | 修改 |
| 6 | 表格展示 | 新增任务标签列 | 新增 |
| 7 | 详情抽屉 | 奖励信息适配现金奖励卡片展示 | 修改 |
| 8 | 投放限制 | 新增司机状态限制开关 + 条件单选 | 新增 |
| 9 | 参与记录 | 新增任务类型列 | 新增 |
| 10 | 参与记录 | 新增奖励发放列（现金奖品显示发放状态 + 打款状态） | 新增 |
| 11 | 参与记录 | 详情抽屉新增任务类型字段、现金打款状态展示 | 新增 |
